<div class="grabable-duties">
	<h1>Grab Duties</h1>
	<div class="container">
    <div class="row">
      <div class="col-md-6 offset-md-3 duty-list">
        <%= form_tag(grab_duties_path, method: :post, id: :"grab-page-form") do %>
          <% format_duties(@grabable_duties).each do |d| %>
            <p>
              <%= check_box_tag "duty_id[#{d[:id]}]", d[:id] %>
              <%= label_tag "duty_id[#{d[:id]}]",  "Time: " + d[:date].to_s + ", " + d[:timing] %>
              <br/>
              <%= label_tag "Location: " + d[:location] %>
            </p>
          <% end %>
          <%if @grabable_duties.length != 0%>
            <p>
              <%= submit_tag "Grab", class: "btn btn-primary", class: "grab", onclick: :"return validateGrabPage();" %>
            </p>
          <%else%>
            <p>
              There is no duty to grab.
            </p>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>