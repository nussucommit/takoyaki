<%= render "availabilities/form", path: "availabilities/places", timeslots: @timeslots %>
<script>
  $(document).on('turbolinks:load', function(){
    document.getElementById("scroll-table").scrollTop = <%= calc_scroll_availabilities_places %>;
  });
</script>
